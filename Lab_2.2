#include <stdio.h>
#include <math.h>

int main() {

    const int a=50;
    const int b=4;
    const int c=9;
    double y;
    
    y = (double) ((c * a) / a + b) + ((b * c) / c + a);
    
    printf("y= %.2f" , y);
    return 0;
}
