#include <stdio.h>
#include <math.h>

int main() {

    const int a=2;
    const int b=2;
    const int c=22;
    double y;
    
    y = (double) ((c * a) / a + b) + ((b * c) / c + a);
    
    printf("y= %.2f" , y);
    return 0;
}
