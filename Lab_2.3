#include <stdio.h>
#include <math.h>

int main() {

    int a, b ;
    double x;

    printf("Введіть значення a = ");
    scanf("%d", &a);
    
    printf("Введіть значення b = ");
    scanf("%d", &b);

    x =(double) (sqrt(a - pow(b,2))) + (sqrt(a * b)) ;
    
    printf("x= %.2f" , x);
    return 0;
}
